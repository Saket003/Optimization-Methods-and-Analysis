import numpy as np

def tableau(A,b,c):
    #Solving the relaxation
    n = len(c)
    x = np.zeros(n)
    table = 0
    #TODO
    flag = 0
    #TODO flag = 1 for unbounded
    return x, table, flag, basis_list
