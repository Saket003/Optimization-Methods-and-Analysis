import numpy as np

def dualsimplex(table):
    x = np.zeros((1,5))#TODO
    flag = 0
    #x_opt give first n only #modify table
    #can modify whole table too - x_opt take and apply same modifications
    flag = 1#if unbounded case
    return x, flag
